package main

import "fmt"

func main()  {
	var day string // Declaration
	day = "Monday"
	fmt.Println("Today is ", day)
	var length, width int // Multiple declaration
	length, width = 10, 20
	fmt.Println(length, width)
	var letter = "A" // Declaration and assignment together
	fmt.Println(letter)
	food := "Cake" // Short variable declaration
	fmt.Println(food)
	//a, b, c := "A", 1, false
	//fmt.Println(a,b,c)
	//_x
	//x, println(x) without assigning a value
	//const myConst int = 8
}
